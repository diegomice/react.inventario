// Importamos React desde la biblioteca 'react'
import React from 'react';

// Definimos el componente funcional Header
const Header = () => {
  // Renderizamos el encabezado
  return (
    // Elemento header que contiene el título del sistema de gestión de inventarios
    <header>
      {/* Título principal del sistema */}
      <h1>Sistema de Gestión de Inventarios</h1>
    </header>
  );
};

// Exportamos el componente Header como exportación predeterminada
export default Header;
